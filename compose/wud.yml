services:
  wud:
    image: getwud/wud:8
    container_name: wud
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - TZ=${TZ}
    networks:
      - proxy

networks:
  proxy:
    external: true
